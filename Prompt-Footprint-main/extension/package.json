{
  "name": "promptfootprint-extension",
  "version": "1.1.0",
  "private": true,
  "description": "PromptFootprint browser extension (local-first, multi-platform)",
  "scripts": {
    "test": "node --test"
  }
}
